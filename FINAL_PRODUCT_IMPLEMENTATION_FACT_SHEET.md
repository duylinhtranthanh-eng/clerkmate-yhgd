# ClerkMate — Final Product Implementation Fact Sheet

> ## ⚠️ Read this first
>
> **The body of this document was verified on 10 September 2026 and the product changed
> substantially on 11 September.** Sections 1–4 below have been corrected; **everything after §4 still
> describes the 10 September build** and must be re-checked before being quoted.
>
> What changed on 11 September, none of which the body reflects:
>
> - The PDF is now **the department's own four-page paper form**, filled in. The old twenty-one
>   section report is kept as a second export.
> - **The grading interface was removed.** No `#/cham-bai`, no faculty screen. The eight processing
>   states and the submission lock remain.
> - **Attachments must produce a sanitized derivative** before anything can be exported; clinical
>   photos must answer whether a face is visible, and a declared face blocks the image.
> - **Several local learner profiles** can share one device, each with its own case list.
> - The level map moved: **APGAR and SCREEM are required from Y2, the genogram from Y5**, and
>   management and follow-up are what SDH adds. Mandatory counts are now Y2 8 · Y5 22 · Y6 34 · SDH 54.
> - Test counts are now **77 / 104 / 49 / 14**, not 55 / 100 / 27.
>
> For numbers that are current, use `FINAL_SUBMISSION_VERIFICATION.md`, verified 11 September.

**Verified:** body 10 September 2026; header and §1–§4 corrected 11 September 2026.
**Method:** every line was checked against the code in `src/`, and where marked *verified* it was also
observed running in a real Chrome against the production build served from a repository subpath.

**Classification:** IMPLEMENTED · PARTIAL · DEMO/SIMULATED · NOT IMPLEMENTED.
Nothing is marked IMPLEMENTED on the strength of reading the source alone.

**Test artefacts** (regenerate with the commands in §34):
`npm test` · `node .shots/verify-final.mjs` · `node .shots/verify-print.mjs`

---

## 1. Product name

**ClerkMate.** Vietnamese tagline: *“Từ ghi chú nhanh đến bệnh án hoàn chỉnh.”*
A mobile-first Family Medicine clinical **learning record** for medical students and postgraduate
trainees. Not a hospital EMR.

## 2. Version

| Item | Value |
|---|---|
| `package.json` version | `0.1.0` (private) |
| Git commit | `108105a` (11 September 2026). The repository exists and is pushed: https://github.com/duylinhtranthanh-eng/clerkmate-yhgd |
| Source size | 77 TypeScript/TSX files, ~19,000 lines in `src/`, plus one `.mjs` serverless function |
| Build output | 9 static files; JS 577 KB raw |

## 3. Public demo URL

**Live on GitHub Pages:**

```
https://duylinhtranthanh-eng.github.io/clerkmate-yhgd/
```

Deployed by GitHub Actions on push to `main` (`npm ci → npm test → build → deploy`), so a failing
audit keeps the previous good build rather than publishing a broken one. The live bundle was compared
against the local build on 11 September and is identical.

The repository name is **not hard-coded anywhere** — Vite uses a relative base (`base: './'`), so
the same artifact works at a domain root or under any subpath.

## 4. GitHub repository URL

**None yet.** The project is prepared for publication (clean `.gitignore`, no secrets, workflow,
README, docs) but `git init` has not been run and no remote exists.

## 5. Netlify backup URL

**<https://clerkmate-yhgd.netlify.app>** — live, working, and kept as the backup and as the only
host that can run the AI proxy. `netlify.toml` is unchanged and still deploys.

## 6. Tech stack

**IMPLEMENTED.** React 19.2 + TypeScript 5.9 (strict, `verbatimModuleSyntax`, `noUnusedLocals`) +
Vite 7. **Two runtime dependencies: `react` and `react-dom`.** Hand-written hash router, hand-written
IndexedDB wrapper, hand-written service worker generated by an inline Vite plugin, two hand-written
CSS files. No router library, no state library, no CSS framework, no UI kit, no PDF library.

## 7. Backend status

**PARTIAL — none for core use; one optional function for AI mode.**

- Core app: **no backend.** No server, no database, no API, no accounts.
- One Netlify Function (`netlify/functions/ai-structure-note.mjs`) exists solely to keep an AI
  provider key off the browser. It is contacted only in AI mode, which is off by default.
- GitHub Pages serves static files only and cannot run it, so a Pages deployment has no backend at
  all and reports AI as unconfigured.

## 8. AI runtime status

**PARTIAL — implemented, validated, and never yet run against a real model.**

No provider key is configured on any deployment, so the endpoint answers `{"configured": false}`
and the AI option cannot be selected. It becomes IMPLEMENTED only after a real provider request
succeeds on a deployed build. **Do not describe it as working.**

What *is* verified: request shaping, response validation, the two guards, consent gating, the
fallback for every failure mode, and that no key reaches the bundle.

## 9. AI provider

| Item | Value |
|---|---|
| Default | Anthropic Messages API (`AI_PROVIDER=anthropic`) |
| Also supported | any OpenAI-compatible chat-completions endpoint (`AI_PROVIDER=openai`) |
| Default model | `claude-sonnet-5` (`AI_MODEL`); `claude-haiku-4-5-20251001` responds faster |
| Key | `AI_API_KEY`, **server-side only**; there is deliberately no `VITE_AI_API_KEY` |
| Currently set | **no** |

## 10. Exactly what is sent to the AI

**Two keys, nothing else** — verified by capturing the outgoing POST body in the browser:

1. `note` — the quick-note text the learner typed, capped at 6,000 characters.
2. `fields` — the static field vocabulary (24 targets: key, Vietnamese label, one-line description).
   Application metadata; contains no patient data (asserted by `npm test`).

**Not sent:** the case record, any other section, other quick notes, attachments or images, the
genogram, the completeness snapshot, the learner's name, student ID, class or level, the case ID, or
any identifier. The captured body was checked for all of these.

## 11. Offline behaviour

**IMPLEMENTED — verified with the server switched off, from a repository subpath.**

Service worker active and controlling, scope `/<repo>/`, all 9 precached URLs inside the subpath. A
real network request fails while the app opens from cache, renders the case list from IndexedDB, and
the local parser still produces suggestions. AI mode disables itself and labels why. No background
retries.

## 12. Local storage

**IMPLEMENTED.** IndexedDB `clerkmate` v1, three stores: `cases` (keyPath `id`, index `updatedAt`),
`blobs` (attachment bytes), `meta` (learner profile, preferences). Autosave on a 450 ms debounce plus
a flush on `pagehide`/`visibilitychange`; no manual save anywhere. `requireExisting` stops a late
flush resurrecting a deleted case. **Not encrypted.** Schema migration merges a stored record onto a
fresh blank one (verified: a record with no `submission` block still opens; an existing one is kept).

## 13. PWA

**IMPLEMENTED — verified under a subpath.** `display: standalone`, `start_url` and `scope` both
resolve to `/<repo>/`, three icons resolve inside the subpath and load 200. Installable on iOS
(Share → Add to Home Screen) and Android. A new build never takes over mid-session: the page offers
“Đã có bản cập nhật — Tải lại”.

## 14. Quick Note

**IMPLEMENTED.** Free-text notepad; save as draft; saved notes listed newest-first with per-note
re-parse and delete, badged with the sections they were filed into. Two sample notes. Disabled with
a stated reason when the case is locked.

## 15. Local parser

**IMPLEMENTED, deterministic, and the default.** `src/parsing/heuristicStructurer.ts`: diacritic-
insensitive normalisation, clause splitting, regex extraction, negation detection, duplicate
detection against the current record. **Not AI.** 28 target fields; dictionaries of 54 conditions,
15 drugs, 9 relations, 19 occupations, 14 red-flag patterns. Verified: every suggestion carries a
snippet, every target has an applier, origin stamped `local`.

Does **not** touch: visit fields, SOCRATES, examination systems, investigations, diagnosis,
management, prevention, follow-up, reflection, genogram layout, or any risk factor.

## 16. AI parser

**PARTIAL** — see §8. Scope, guards and behaviour:

- Same `NoteStructurer` interface, same `StructuringSuggestion` shape, same `apply.ts` appliers,
  same review sheet. **The learner still ticks each item and presses “Đưa N mục vào bệnh án”.**
- **24 allowed targets**, filtered twice (proxy against the list it was sent; browser against its
  own catalogue). Verified: `diagnosis.primary` is discarded, as is any unknown target.
- **Every suggestion must quote the note.** The browser checks the quoted span actually occurs in
  the note. Verified: a fabricated red flag sourced to a sentence not in the note is discarded, and
  a suggestion with no quote at all is discarded.
- A value the payload builder cannot map is discarded rather than guessed (verified for an
  unparseable blood pressure and an unparseable age).
- Confidence 0–1 maps to the three UI tiers; low-confidence items are **not pre-ticked**.
- **No AI target exists for** diagnosis, differentials, reasoning, management, prescriptions,
  investigations, examination systems, risk factors or scales — asserted by `npm test`.
- **Limit stated plainly:** the guards check provenance, not clinical judgement. A model that
  correctly quotes “Không dị ứng thuốc” and proposes an allergy passes both guards. That case was
  tested: it arrived low-confidence, unticked, with the contradicting quote visible. The human
  review step is what catches this class of error.

## 17. Structured record

**IMPLEMENTED — 18 sections** in 8 groups, each badged live by the completeness engine.
History (HPI, 8 SOCRATES fields, red flags as 6 groups / 31 items in two independent lists, ICE,
systems review) · past/personal history with one-tap “asked, nothing there” · lifestyle (8 fields) ·
family history · Family Medicine assessment (7 family types, 10 life-cycle stages, Family APGAR with
interpretation, SCREEM 6 domains × resources/pathology, home environment, continuity) · examination ·
investigations · attachments · risk review · diagnosis · management · medications · prevention
(10 screening + 7 vaccination items) · SOAP follow-up · learner reflection.

## 18. Completeness

**IMPLEMENTED.** Pure function; never writes to the record. 66 requirement predicates; weighted
percentage (mandatory 3 / recommended 2 / optional 1). Verified by `npm test` and in the browser:

- a blank case is never 100% and satisfies 0 mandatory items at every level;
- the percentage equals the documented weighted formula, and tier totals equal the item list;
- **11 requirements are conditional** — a non-applicable one leaves the denominator rather than
  counting as a failure, so totals are **per case, not per level**;
- editing raises the percentage immediately;
- every missing item has a section rollup, and clicking one navigates to that section (verified).

**Privacy blockers are separate from the score, by design, but not from submission eligibility.**
Attachment redaction is a completeness requirement only from Y5 up *and* an independent submit
blocker; the gate raises both (verified).

## 19. Learner levels

**IMPLEMENTED — 4 levels, cumulative, verified by `npm test`.**

| Level | Requirements | Mandatory | Recommended | Advanced |
|---|---|---|---|---|
| Y2 — Tiền lâm sàng | 15 | 6 | 5 | 4 |
| Y5 — Lâm sàng YHGĐ | 38 | 19 | 14 | 5 |
| Y6 — Thực hành tổng hợp | 59 | 31 | 21 | 7 |
| SDH — Sau đại học | 66 | 50 | 13 | 3 |

Each level covers everything the level below covers (asserted). Gender rule verified (reproductive
history applies to a female patient, not a male one). Age rule verified (cardiovascular risk applies
from 40 and when age is unknown, not at 30). **Previewing another level does not mutate the record
or change its declared level** — asserted in `npm test` and confirmed in the browser. The declared
level is self-declared and unenforceable; the printed record carries a per-page watermark, a level
stamp, the full declared-level history and a warning if the level changed after the case began.

## 20. Genogram

**IMPLEMENTED for three generations.** Deterministic layout; inline SVG, so it prints as vector
(confirmed in the PDF). Square/circle/diamond, X for deceased, double border for the index patient.
Eight hatch patterns assigned by Vietnamese-collated condition name with a printed legend, max 4
bands per person. Patient identity read from the administrative section, never stored twice.
Life-cycle suggestion is rule-based, shows its reasoning and must be accepted.
**NOT IMPLEMENTED:** grandparents, half-siblings, adoption, twins, pregnancy loss, emotional
relationship lines. Stated in the UI.

## 21. Risk review

**IMPLEMENTED — 9 domains, 57 factor definitions** (asserted; ids unique, every factor in a declared
domain). Per-factor “why we ask” line, per-domain bulk yes/no, custom factors, patient-specific
filtering (18 of 57 have applicability predicates). Scaffolding fades by level — Y2 checklist, Y5
recall-then-checklist, Y6/SDH generate — with two deliberate exceptions asserted by `npm test`: the
emergency domain keeps its checklist at **every** level, and the social domain reads from SCREEM.
Seven scales: PHQ-2/9, GAD-2/7, Mini-Cog ship items; **HADS and ISI ship no items** (licensed —
asserted) and take an entered score. Escalation from a positive screen; PHQ-9 item 9 raises a safety
notice. Graded falls assessment (STEADI + Timed Up and Go) with the banding rule printed on screen
and asserted in tests. **Cardiovascular risk is NOT calculated** — inputs, chart used and the
learner's own reading are recorded.

## 22. Clinical examination

**IMPLEMENTED.** Nine vital-sign inputs plus a computed BMI on **WHO Asia-Pacific** cut-offs
(asserted: 155 cm/62 kg → “Béo phì độ I”). Ten organ systems, three states — *Chưa khám /
Bình thường / Bất thường*. One-tap normal writes that system's standard sentence; “Tất cả bình
thường” never overwrites an abnormal system or learner-typed text; *Chưa khám* is never treated as
normal.

## 23. Attachments

**IMPLEMENTED.** Camera or library, multiple files, 5 categories, per-image title/date/category/note,
client-side thumbnails. Two independent flags (`redacted`, `privacyChecked`); an unprocessed image is
tagged red on the thumbnail, warned about on the attachments screen and on the export screen, and
listed with its status in the PDF. A photographed result can be attached to the specific result row.
**No OCR** — values are typed. All six blob write paths are lock-guarded (§27).

## 24. Redaction

**IMPLEMENTED and destructive — verified end to end.**

Draw boxes by dragging; move a box by dragging inside it; resize from a corner handle; several boxes
per image; a “Che dải trên” helper creates a starting box; ①/② step indicator. Verified in a real
mobile-emulated browser with genuine pointer events: a box can be created, moved and a second added.

| Check | Result |
|---|---|
| Canvas coordinates track the layout, not the raw image | ✅ canvas attribute width equals its CSS width |
| Applying rewrites the stored blob **in place** | ✅ same blob key, different SHA-256 |
| The original bytes are not kept alongside | ✅ exactly one blob in the store afterwards |
| Covered region is solid black at full resolution | ✅ >90% pure black in the sampled band of the stored image |
| Thumbnail regenerated | ✅ |
| Flags set to redacted + privacy-checked | ✅ |
| Preview and PDF use the redacted image | ✅ printed thumbnail byte-matches the stored redacted thumbnail |
| Backup and restore preserve the redacted image | ✅ SHA-256 identical after a wipe-and-restore |
| Submission blocker detects an unconfirmed attachment | ✅ named blocker, submit disabled |

Described only as **privacy support / identifier redaction**. No regulatory claim is made. An escape
hatch exists for images that genuinely carry no identifiers.

## 25. PDF

**IMPLEMENTED — a real sample was generated and inspected (27/27 checks).** No PDF library: the
browser print pipeline plus `print.css`.

Verified in print media: top bar, tab bar and all `.no-print` blocks hidden; watermark and footer
shown; no sideways scroll; no element inside the record overflows; Vietnamese diacritics correct;
learner name/ID/class, patient data, level stamp, submission code and reopen count all present; the
genogram as vector SVG; the attachment list self-consistent with its own privacy status; the
watermark carrying level + student ID on **every** page.

Real PDF: 832 KB, **10 pages**, 19 embedded image objects, 80 embedded font objects, valid
header/trailer. Pages 1, 4, 9 and 10 were opened and read: sensible page breaks, no clipped text,
and the redacted band is solid black in the printed image.

Up to 21 numbered sections exist; empty ones hide themselves, so a given PDF shows fewer.

## 26. Backup / restore

**IMPLEMENTED — verified by the full round trip the task asks for.**

One JSON file holding the profile, every case and every attachment blob as a data URL. Restore
decodes data URLs **in-process** (`atob` → `Uint8Array` → `Blob`), not via `fetch`, so the deployed
Content-Security-Policy cannot block it. Failures are counted and reported rather than swallowed.

Round trip: export → **wipe both IndexedDB stores** → restore.

| Check | Result |
|---|---|
| Backup carries the attachment bytes | ✅ |
| Local data really was wiped first | ✅ 0 cases, 0 blobs |
| Every case comes back | ✅ |
| Case content byte-identical | ✅ full JSON comparison |
| **Attachment image bytes survive** | ✅ SHA-256 identical |
| Redacted state preserved | ✅ |
| Family members / genogram data preserved | ✅ |
| Submission metadata preserved | ✅ |
| Completeness snapshot preserved | ✅ |
| Restore reports what it restored | ✅ “Đã khôi phục N ca, M ảnh.” |

Backward compatible: the format is unchanged, so older backups still restore.

## 27. Submission lock

**IMPLEMENTED — audited across every mutation path.**

Gate → submit → lock → optional reopen. Submitting mints a code (`<studentId>-<YYMMDD>-<nnn>`),
stamps the time, locks the record and downloads a submission bundle. Enforced in **both** write paths
of the editor hook (`update` and `replace`); the two transitions that must write to a locked record —
reopening, and marking a comment read — go through a separately named `replaceWorkflow` so every
bypass is visible at its call site. The six attachment blob paths, which do not go through the editor
at all, are guarded individually.

Verified in the browser on a locked case:

- **17 section editors** and **4 tabs** swept — every enabled input, textarea, chip and small button
  fired (15–63 attempts per section, 400+ in total). The case content fingerprint and all image
  hashes were unchanged after every single one.
- Attachment blob paths: add, sample-insert, redact, delete and privacy-confirm are all disabled,
  and the screen says why.
- **Enforcement survives the UI being forced open**: `disabled` stripped in the DOM and the real
  handlers fired — nothing was written.
- Reopen unlocks, is counted, and clears the submitted stamp so the next send is visibly a
  resubmission; editing works again immediately afterwards.
- Reopen count and the full feedback history are printed in the PDF.

## 28. Teacher grading

**IMPLEMENTED, without authentication — and it says so.**

The reviewer opens the learner's bundle at `#/cham-bai` in the same app: learner identity, submission
code, completeness, missing mandatory items, reopen count, and the whole record read-only. They type
a name and a comment and choose *Trả lại để bổ sung* (comment required) or *Chấp nhận*; the app
writes the decision into the record and downloads the result. **Nothing is stored on the reviewer's
device.** The learner imports the returned file; **only the submission block is merged**, so a
reviewer cannot alter clinical content (asserted by `npm test` against a deliberately tampered file).
A returned case is badged, announced, shown the comment verbatim, and unlocked. Feedback history
survives multiple rounds.

**Eight statuses**, five derived from the record's own content. A reviewer's decision governs the
badge until the learner answers it, tracked **by review id, not by timestamp** — the two sides are
different devices with their own clocks, and this bug (a reviewer whose clock ran slow having their
decision silently ignored) was caught by `npm test` and fixed.

## 29. Demo cases

**DEMO/SIMULATED — two fully worked fictional cases**, both at SDH, seeded in one tap and verified to
work on a **fresh profile with empty storage**.

- **Bà H. (giả lập), 58, homemaker** — right knee pain 3 months on 10-year hypertension; nuclear
  family, empty-nest stage, Family APGAR and SCREEM filled, three-generation genogram. Ships one
  deliberately **un-redacted** simulated lab slip so the redaction tool can be tried.
- **Ông T. (giả lập), 74, retired, living alone** — two falls in three months, seven active problems
  with control status, polypharmacy, orthostatic hypotension, PHQ-2 positive → PHQ-9, ISI, Mini-Cog,
  STEADI high (TUG 15.5 s). Ships an already-redacted slip.

The slips are **drawn by the app on a canvas at seed time** — no image file in the repository and no
real document. Each carries the line *“Ảnh mô phỏng dùng cho demo ClerkMate — số liệu và thông tin
đều giả lập.”* and takes its name and birth year from the case it is attached to.

## 30. Known limitations

- Requirement levels are the developers' demo rules. **No teaching faculty has reviewed them and
  there has been no study of educational effect.**
- Genogram: three generations only.
- Diagnosis coding: a 20-entry shortlist, not full ICD-10 / ICPC-2.
- No OCR, no speech input, no cloud sync, no cohort dashboard, no search or filtering across cases.
- Cardiovascular risk is not calculated.
- HADS and ISI items are not reproduced; PHQ/GAD/Mini-Cog Vietnamese wording is a working
  translation, not a validated one. MMSE and MoCA are excluded.
- Reviewer workflow has no authentication and no tamper-proofing; the file moves manually.
- One case is one visit plus follow-up entries; there is no patient timeline across cases.
- AI mode needs the internet and a configured provider, and cannot run on GitHub Pages.
- No measured evaluation of time saved or error reduction. Any figure is an estimate.

## 31. Known unresolved bugs

The two serious bugs found on 09/09/2026 were fixed and re-verified on the deployed build; a third
was found by the new test suite on 10/09/2026 and fixed. What remains is cosmetic or dormant:

| # | Issue | Severity |
|---|---|---|
| 1 | Genogram life-cycle button says “Ẩn danh sách 8 giai đoạn” while the list holds 10 | cosmetic |
| 2 | `patient.dateOfBirth` and `managementPlan.referral.urgency` are editable but never printed | minor |
| 3 | Dead model fields with no UI: `patient.phone`, `reproductive.gravida` / `.abortions` / `.livingChildren`, `quickNote.archived` | minor, inflates any field count taken from the types |
| 4 | Reflection “Từ khóa” hints at finding cases later, but no search or filter exists anywhere | minor, misleading copy |
| 5 | `#/case/<id>/s/genogram` renders a fallback string; both in-app links route to the tab instead | cosmetic, URL-only |
| 6 | Long names are truncated in genogram labels (“Bà H. (giả lộ…”) to avoid collisions | cosmetic, by design |
| 7 | The attachment thumbnail in the PDF catalogue is cropped by the 3-column grid | cosmetic |
| 8 | CSS class `.doc` is used both by the printed article and by definition lists inside UI cards | code hygiene, no user impact |
| 9 | Deleting a submitted case is still allowed (the lock governs editing, not deletion) | by design, undocumented |

**Fixed on 10/09/2026:** all six attachment blob write paths bypassed the submission lock (redacting
an image on a submitted case would have overwritten its pixels irreversibly); the status machine
compared two devices' clocks; the demo attachment note contradicted its own privacy status in the
exported PDF.

## 32. Privacy limitations

- IndexedDB is **not encrypted**. Anyone who can open the browser profile can read the records.
- Clearing site data destroys everything; backup is manual.
- **In AI mode the note text leaves the device** and is processed by a third party. ClerkMate makes
  no claim about that provider's retention, training use or jurisdiction. The in-app restriction to
  fictional or de-identified data is a written instruction, **not a technical control** — the app
  cannot tell whether a note is fictional.
- Redaction is destructive for the copy ClerkMate keeps. It cannot affect copies made elsewhere
  (camera roll, cloud photo backup) before the image was imported.
- The reviewer flow is unauthenticated; a submission bundle can be read and edited by anyone holding
  the file.
- No security or privacy review has been carried out by anyone. **No regulatory compliance is
  claimed.**
- Verified: no console output contains quick-note text, case data, learner name or student ID; and
  during a full end-to-end run in core mode the app made **zero** network requests.

## 33. Deployment limitations

- **GitHub Pages is not live yet** — no repository exists. Everything needed is in place and was
  verified against the Pages hosting shape locally; §34 lists the commands.
- GitHub Pages serves static files only, so **AI mode cannot work there**. It degrades correctly:
  the probe 404s, the app reports “chưa cấu hình” and hides the option.
- Netlify remains the only host that can run the AI proxy, and the only one that sends the
  Content-Security-Policy headers.
- The Pages workflow needs one manual step in the repository UI: **Settings → Pages → Source:
  GitHub Actions**. No token or secret is required.
- No licence has been chosen; until one is added the code is not licensed for reuse.
- `netlify-cli` is a heavy devDependency and carries 5 high-severity advisories through `sharp`.
  **Runtime dependencies have 0 advisories.** CI installs it needlessly.

---

## CLAIM · STATUS · EVIDENCE

| Claim | Status | Evidence |
|---|---|---|
| “Runs offline after the first load” | **VERIFIED** | Server stopped; a real request failed while the shell opened from cache, 1 case rendered from IndexedDB and the local parser produced suggestions — at a repository subpath |
| “Stores data locally, no cloud” | **VERIFIED** | IndexedDB only; zero network requests across a full end-to-end run in core mode; every request that did occur stayed on the origin |
| “No login, no account” | **VERIFIED** | No password field, session or token anywhere; first run asks only for a name and an id and says it is not a login |
| “Installable PWA” | **VERIFIED** | Service worker active, scope `/<repo>/`, 9 precached files all inside the subpath, manifest `start_url`/`scope`/3 icons resolve and load, `display: standalone` |
| “Quick notes become a structured record” | **VERIFIED** | Local parser returns >3 suggestions on the sample note, each with a source snippet; 28 target fields; nothing written before confirmation |
| “Suggestions always show their source” | **VERIFIED** | Every suggestion card carries a `.snippet`; AI suggestions with no quote, or a quote absent from the note, are discarded |
| “Completeness checked by learner level” | **VERIFIED** | 15/38/59/66 requirements with 6/19/31/50 mandatory; weighted formula reproduced; conditional rules for sex, age, attachments and results |
| “Previewing another level does not change the case” | **VERIFIED** | Record unchanged byte-for-byte across all four previews; declared level stays SDH |
| “Genogram is generated, not drawn by hand” | **VERIFIED** | Deterministic layout; renders as vector SVG in the exported PDF with the condition legend |
| “Redacts identifiers destructively” | **VERIFIED** | Same blob key, different SHA-256, one blob in the store, >90% pure black in the sampled band at full resolution, and the same redacted bytes in preview, PDF, backup and restore |
| “Submission locks editing” | **VERIFIED** | 17 sections + 4 tabs swept with 400+ mutation attempts, all six blob paths guarded, and enforcement holds with the UI forced open |
| “Reopening is recorded and visible” | **VERIFIED** | Reopen count stored, submitted stamp cleared, count printed in the PDF |
| “Backup restores attachments” | **VERIFIED** | Export → both stores wiped → restore: cases byte-identical, attachment SHA-256 identical, redacted state and submission metadata preserved |
| “Teacher can return work with a comment” | **VERIFIED** | Full round trip: bundle → grading screen → returned file → import → badged, announced, comment verbatim, unlocked |
| “A reviewer cannot alter clinical content” | **VERIFIED** | A deliberately tampered reviewer file was merged; only the submission block came through |
| “Exports a PDF with correct Vietnamese” | **VERIFIED** | Real 10-page PDF inspected: diacritics correct, 80 embedded fonts, no clipping, sensible breaks, watermark on every page |
| “No secrets in the repository” | **VERIFIED** | 104 tracked files scanned against 11 secret and personal-data patterns: 0 hits; `dist/` carries no key, no `x-api-key`, no provider host |
| “AI only structures, never diagnoses” | **VERIFIED (by construction)** | No AI target exists for diagnosis, management, prescriptions, investigations, examination or risk — asserted in `npm test`; out-of-catalogue targets discarded twice |
| “AI fails safely” | **VERIFIED** | Offline, malformed output, HTTP 401, 429 and 500 each fell back to the local parser with the stated message and the note preserved |
| “AI sends only the note” | **VERIFIED** | Captured POST body contained exactly `note` and `fields`; no record, image, identity or case id |
| **“AI-assisted note structuring works”** | **NOT VERIFIED** | No provider key is configured; no real model request has ever succeeded. Built and tested against a stub only |
| **“Live on GitHub Pages”** | **NOT VERIFIED** | No repository exists yet; verified only against the Pages hosting shape locally |
| “Educationally validated” | **NOT CLAIMED** | No faculty review, no controlled study, no outcome data |
| “Secure / regulation-compliant” | **NOT CLAIMED** | IndexedDB unencrypted, no third-party review, no compliance assessment |
| “Saves X% of time” | **NOT CLAIMED** | No measurement has been performed |

---

## 34. Reproducing every result above

```bash
npm ci
npm test                 # 55 rule checks, no browser needed
npm run build            # production build

# 100 end-to-end checks, served from a repository subpath (the GitHub Pages shape)
npm run preview:pages &
npm run verify:app

# 27 print checks plus a real sample PDF written to /tmp/clerkmate-print/
npm run preview:pages &
npm run verify:print
```

All three harnesses are committed: `scripts/audit.mjs`, `scripts/verify/verify-final.mjs` and
`scripts/verify/verify-print.mjs`. They need nothing but Node and a local Chrome — no test framework
and no extra dependency. `scripts/verify/stub-provider.mjs` and `scripts/verify/aitest-server.mjs`
exercise the AI proxy end to end without a real provider key.
